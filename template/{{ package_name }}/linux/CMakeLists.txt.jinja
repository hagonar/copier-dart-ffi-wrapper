# {{ package_name }} - Linux platform support
#
# Native libraries are handled automatically by Dart Build Hooks (hook/build.dart).
# This file is kept for Flutter plugin structure compatibility.

cmake_minimum_required(VERSION 3.10)
project({{ package_name }} LANGUAGES CXX)
